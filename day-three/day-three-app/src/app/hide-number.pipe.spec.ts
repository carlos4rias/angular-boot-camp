import { HideNumberPipe } from './hide-number.pipe';

describe('HideNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new HideNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
